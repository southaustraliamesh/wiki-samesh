name: Deploy Cloudflare Pages

on:
  push:
    branches:
      - main
      - preview
  workflow_dispatch:

permissions:
  contents: read
  deployments: write

concurrency:
  group: wiki-samesh-pages-${{ github.ref }}
  cancel-in-progress: true

jobs:
  test-and-deploy:
    name: Test, build and publish Docusaurus wiki
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v5

      - name: Setup Node.js
        uses: actions/setup-node@v5
        with:
          node-version: 24
          cache: npm

      - name: Install dependencies
        run: npm ci

      - name: Run tests
        run: npm test

      - name: Build Docusaurus static artifact
        run: npm run build

      - name: Verify artifact
        run: |
          test -f build/index.html
          test -f build/img/samug-logo.png
          test -f build/img/favicon.ico
          test -f build/img/favicon-32.png
          test -f build/img/favicon-192.png
          test -f build/robots.txt
          test -f build/sitemap.xml
          test -f build/meshcore/south-australia.html
          test ! -e build/.git
          test ! -e build/node_modules

      - name: Ensure Cloudflare Pages project exists
        env:
          CLOUDFLARE_API_TOKEN: ${{ secrets.CLOUDFLARE_API_TOKEN }}
          CLOUDFLARE_ACCOUNT_ID: ${{ secrets.CLOUDFLARE_ACCOUNT_ID }}
        run: npx --yes wrangler@4.118.0 pages project create wiki-samesh --production-branch=main || true

      - name: Deploy to Cloudflare Pages
        env:
          CLOUDFLARE_API_TOKEN: ${{ secrets.CLOUDFLARE_API_TOKEN }}
          CLOUDFLARE_ACCOUNT_ID: ${{ secrets.CLOUDFLARE_ACCOUNT_ID }}
          CF_PAGES_BRANCH: ${{ github.head_ref || github.ref_name }}
        run: npx --yes wrangler@4.118.0 pages deploy build --project-name=wiki-samesh --branch="$CF_PAGES_BRANCH" --commit-dirty=true

      - name: Ensure production custom domain
        if: github.event_name == 'push' && github.ref == 'refs/heads/main'
        env:
          CLOUDFLARE_API_TOKEN: ${{ secrets.CLOUDFLARE_API_TOKEN }}
          CLOUDFLARE_ACCOUNT_ID: ${{ secrets.CLOUDFLARE_ACCOUNT_ID }}
          CF_PAGES_PROJECT: wiki-samesh
          CF_PAGES_DOMAIN: wiki.samesh.au
        run: |
          python3 - <<'PY'
          import json
          import os
          import urllib.error
          import urllib.request

          account = os.environ['CLOUDFLARE_ACCOUNT_ID']
          token = os.environ['CLOUDFLARE_API_TOKEN']
          project = os.environ['CF_PAGES_PROJECT']
          domain = os.environ['CF_PAGES_DOMAIN']
          base = f'https://api.cloudflare.com/client/v4/accounts/{account}/pages/projects/{project}/domains'
          headers = {
              'Authorization': f'Bearer {token}',
              'Content-Type': 'application/json',
          }

          def request(method, url, body=None):
              data = None if body is None else json.dumps(body).encode()
              req = urllib.request.Request(url, data=data, headers=headers, method=method)
              with urllib.request.urlopen(req, timeout=30) as response:
                  return json.load(response)

          try:
              existing = request('GET', base).get('result') or []
              names = {item.get('name') for item in existing if isinstance(item, dict)}
              if domain in names:
                  print(f'Cloudflare Pages custom domain already present: {domain}')
              else:
                  result = request('POST', base, {'name': domain})
                  if not result.get('success'):
                      raise SystemExit(f'Cloudflare API did not accept custom domain {domain}: {result}')
                  print(f'Cloudflare Pages custom domain requested: {domain}')
          except urllib.error.HTTPError as exc:
              payload = exc.read().decode('utf-8', 'replace')
              if exc.code in (409, 400) and domain in payload:
                  print(f'Cloudflare Pages custom domain appears to already exist: {domain}')
              else:
                  raise SystemExit(f'Cloudflare Pages custom domain setup failed for {domain}: HTTP {exc.code} {payload}')
          PY
